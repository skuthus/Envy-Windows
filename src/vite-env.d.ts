/// <reference types="vite/client" />

// Declares the module types for asset imports — `import icon from './x.png'`
// resolving to a URL string. Needed since the About page uses the app's real
// icon file rather than a drawing of it.
